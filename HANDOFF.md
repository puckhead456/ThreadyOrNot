# Handoff: Thready or Not

Crochet, cross-stitch and sewing counter PWA (crochet first; crafts plug in via `docs/CRAFTS.md`). Live: https://puckhead456.github.io/ThreadyOrNot/ (repo puckhead456/ThreadyOrNot, deploys from `main` via `.github/workflows/pages.yml`). Local folder `C:\Users\mitch\CrochetBs` (folder name intentionally unchanged). Plain HTML/CSS/JS, no build step. Everything important is documented in `SPEC.md`; read it first. Owner: puckhead456 (Windows user "mitch"). Started 2026-09-14.

## Status (updated 2026-09-15, evening)
- `main` = what is live (single branch; the owner wants everything on main). Features: counters, parts, templates (editable), checklists, pattern paste + in-app PDF import (pdf.js, on-device), parser v2.1, 6 themes, celebrations, guided tours + help, export/import, Stardrop Night default theme, proprietary LICENSE.
- **Live 3D diagram, second pass (deployed 2026-09-15, cache v11):** the stitch button counts on release inside a 12 px tolerance (nothing on press, no undo in the gesture); a swipe on the button spins the piece and never counts; drag direction follows the finger in button and viewer (pitch was inverted); `Diagram.dragStart/dragMove/dragEnd` host-driven API (v1.1.0); yarn material pass (warm key / cool fill, sheen, per-stitch noise, watertight bands, darker wrong side, contact shadow, calmer ghost rings). ≤ 0.2 ms/frame JS on 60×60. Not checked on real touch hardware or a mid-phone GPU.
- **Crafts expansion (built 2026-09-15, deployed as cache v13; parser tuning v14; deferred items v15):** the app now serves three crafts. Contract in `docs/CRAFTS.md`; research + per-craft specs in `docs/research/cross-stitch.md` and `docs/research/sewing.md`. `Project.craft` ('crochet' default; old saves migrate), opaque `Project.craftData`, `Store.registerCraft` / `App.registerCraft`, `#screen-craft`, craft picker in New project (only shows when a craft module registers), `js/blobstore.js` (IndexedDB for chart page images), `PdfText.open`, `Tour.register`, `Settings.crafts`. Crochet code paths untouched.
  - Cross-stitch (`js/xstitch.js`, `js/app-xstitch.js`, `css/xstitch.css`): OXS import/export (full cell data), PDF import (colour-key parser with confidence banner + chart pages rendered to BlobStore, image-only fallback for scans), manual entry, chart viewer with marking tools, colour key, tap counter with live chart preview in the button, floss/fabric/pages/parking sheets, 4 FAQs + tour. Shipped 2026-09-16: printable chart export (`XStitch.printableHTML`, inline-SVG tiles in mm), **PDF grid reader beta** (`XStitch.extractGrid` reads per-stitch coloured rectangles from the page operator list; all 7 KG-Chart fixtures recover exactly in 0.4–8 s; Spriter/DMC-library PDFs fail fast), photo crop frame. Deferred: tracking of backstitch/fractionals (modelled, drawn, not markable); a "craftData over 1 MB" warning (a recovered 298×500 chart is ~290 KB).
  - Photo → chart (`js/xstitch-photo.js`): fully on-device Web Worker (k-means in CIELAB → nearest DMC by CIEDE2000 → confetti cleanup); ~90–145 ms for 200×250 / 24 colours on desktop; deterministic, cancellable. No LLM, no network, no per-user cost. `test/xstitch-photo.test.html` has a visual QA panel.
  - Sewing (`js/sewing.js`, `js/app-sewing.js`, `css/sewing.css`): instruction-booklet parser (steps, cutting list incl. quilting WOF/subcut, notions, seam allowance with exceptions, size charts, fabric, kind detection) → step counter with big Step-done button, cutting/notions/size/fabric/machine/seam-allowance sheets, copy shopping list, quilt unit counters, import review list, 6 FAQs + tour. Shipped 2026-09-16: opt-in step page images (BlobStore, page viewer sheet, ~170 kB/page), cutting-table SVG illustration + step ring, Awake toggle via `ctx.wake`.
  - Tests (all green 2026-09-16): patterns 335, patterns.fixtures 156, crafts 124, blobstore 46, xstitch 492, sewing 596, xstitch-photo 131, xstitch.fixtures 174 (11 real charts), sewing.fixtures 114 (2 real booklets). The fixtures pages read the PDFs in gitignored `tmp-pdf/` DIRECTLY via PdfText (sessionStorage cache; a `.txt` of the same name overrides), so adding a fixture = drop `xs-*.pdf` / `sew-*.pdf` in and add shape assertions. Real files on disk (see `tmp-pdf/SOURCES.md`): 7 KG-Chart LE charts (Pac-Man, Portal, Charmander typography, 4 Pokémon quadrants), 1 Spriter chart, 3 DMC library PDFs (one is embroidery and is flagged as such), 2 Pattern Runway booklets (pencil skirt, sundress; their cutting lists live on the pattern sheets, so "no cutting list" is the correct result). Still wanted: an OXS+PDF pair from FlossCross, an Artecy-style commercial chart, quilt/bag booklets.
  - Grid extraction (shipped as beta): the text-layer glyph lead was wrong (only ~7% of cells have text glyphs); KG-Chart draws every stitch as four quarter-rectangles filled with the exact floss RGB, which `extractGrid` reads from `page.getOperatorList()`. Text glyphs remain a second lattice source for generators that do emit one per stitch.
  - Known nits: `ctx.pdfDropZone` with `onPages`+`onText` skips the cross-page running-head pass (documented in CRAFTS.md); the service worker does not register inside the desktop Browser pane (environment) BUT when it does register (real browsers) it is cache-first for `js/*.js` on the test pages too, so clear it before running tests; `requestAnimationFrame` is throttled in the pane unless a screenshot forces a frame.
- Closed-source decision: stay public for now (GitHub Pages free tier needs a public repo). Plan: move hosting to Cloudflare Pages (free, private repo, custom domain) when the business plan's launch checklist starts, then flip the repo private. Before launch also: name/trademark search, rename IP-referencing themes and redraw two mascots, add minification + canary strings (note: `js/xstitch-photo.js` builds its Worker from `String(fn)`, so `XSPhotoCore` / `xspWorkerBody` must be excluded from name mangling), later move parser/diagram-model behind an API for Pro.
- Business plan artifact: https://claude.ai/artifact/YM4khtjkszxfibJDyLNVyq (private).
## How to work on it
- Local server: `.claude/launch.json` config `stitchkeeper` runs `tools/serve.ps1` on http://localhost:8765 (no node or python on this machine).
- The service worker is cache-first. Before testing ANY change in the browser: unregister service workers and delete caches from the console, then reload with a cache-busting query. Bump `CACHE_VERSION` in `sw.js` on every release and precache any new file. (Currently v9.)
- Tests: `test/patterns.test.html` (unit, committed synthetic snippets; 335), `test/patterns.fixtures.html` (runs against the real pattern PDFs in `tmp-pdf/`, which is gitignored and copyrighted: never commit those; 156), `test/celebrate.test.html`, `test/diagram.test.html`.
- Git is signed in; pushes work non-interactively with `GIT_TERMINAL_PROMPT=0` and `GCM_INTERACTIVE=never`. Put commit messages in a short-path file such as `C:\Users\mitch\AppData\Local\Temp\sk-commit.txt` and use `git commit -F`; long scratchpad paths break git on Windows. Git Bash heredocs work; PowerShell here-strings with quotes do not survive as git arguments.
- Deploy = push to `main`; the Pages workflow runs in ~1 minute. Verify live files WITHOUT cache-busting query strings (GitHub's CDN returns 404 for `?x=` on fresh deploys).
- The Browser pane's localStorage is a scratch profile: agents may wipe it. The owner's phone data is separate.
- Delegation pattern that has worked: write the contract into SPEC.md, spawn one Opus agent per independent file group (tell them not to touch each other's files and to clear the service worker before testing), integrate, verify at 375px in the Browser pane, deploy.
- New pattern PDFs: copy to `tmp-pdf/`, extract text via `tmp-pdf/extract.html` (or `PdfText` in-app), add a `.txt` fixture and assertions to `test/patterns.fixtures.html`, extend the parser via the parser agent, keep everything green.
