/* Thready or Not — js/app-xstitch.js
 * Cross-stitch UI. Registers itself with App.registerCraft({ id: 'crossstitch', … })
 * at script-evaluation time; see docs/CRAFTS.md for the contract and
 * docs/research/cross-stitch.md for the screens.
 *
 * Stub: the shell plumbing landed first, so this file registers nothing yet.
 * Until it does, a cross-stitch project shows the shell's "needs the module"
 * card and crochet is completely unaffected.
 */
(function () {
  'use strict';
})();
